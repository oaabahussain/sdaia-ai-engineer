# npm audit high-severity report

Generated: 2026-09-07

`npm audit` metadata on release commit `059b90fca83d20b950b5c83e751a55180387fd53`:

- info: 0
- low: 0
- moderate: 10
- high: 5
- critical: 0
- total: 15

High-severity packages reported by npm:

1. `@faker-js/faker` — advisory: Faker `helpers.fake` can lead to arbitrary code execution (`GHSA-qxc2-j82w-r537`).
2. `@opentelemetry/propagator-jaeger` — includes the Jaeger propagator malformed-header denial-of-service advisory (`GHSA-45rx-2jwx-cxfr`) and inherits risk through `@opentelemetry/core`.
3. `@opentelemetry/sdk-trace-node` — high severity through `@opentelemetry/core`, `@opentelemetry/propagator-b3`, `@opentelemetry/propagator-jaeger`, and `@opentelemetry/sdk-trace-base`.
4. `@redocly/cli` — high severity through transitive OpenTelemetry packages and `@redocly/respect-core`.
5. `@redocly/respect-core` — high severity through `@faker-js/faker` and `undici`.

These findings are in development/tooling dependencies used for API linting and validation. This report records the current dependency risk; it does not automatically apply potentially breaking dependency upgrades.
