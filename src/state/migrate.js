function remapKeyedObject(value,map){return Object.fromEntries(Object.entries(value||{}).map(([k,v])=>[map[k]||k,v]))}
function remapActiveExam(exam,map,meta){if(!exam)return null;return{...exam,track_id:meta.trackId,track_version:meta.trackVersion,exam_profile_id:meta.examProfileId,exam_profile_version:meta.examProfileVersion,questionIds:(exam.questionIds||[]).map(id=>map[id]||id),answers:remapKeyedObject(exam.answers,map),confidence:remapKeyedObject(exam.confidence,map),flags:remapKeyedObject(exam.flags,map),optionOrders:remapKeyedObject(exam.optionOrders,map)}}
export function migrateState(raw,{anonId,questionIdMap={},trackId,trackVersion,examProfileId,examProfileVersion}){if(raw?.version===2)return raw;const source=raw&&typeof raw==='object'&&!Array.isArray(raw)?raw:{};const exam=source.examV2||{};const now=new Date().toISOString();return{version:2,anon_id:source.anon_id||anonId,created_at:source.created_at||now,updated_at:source.updated_at||now,preferences:{lang:exam.lang==='en'?'en':'ar',theme:exam.theme==='dark'?'dark':'light'},tracks:{[trackId]:{track_version:trackVersion,active_exam:remapActiveExam(exam.active,questionIdMap,{trackId,trackVersion,examProfileId,examProfileVersion}),exam_history:Array.isArray(exam.history)?exam.history.map(item=>({...item,legacy_summary:true,track_id:trackId,track_version:trackVersion,exam_profile_id:examProfileId,exam_profile_version:examProfileVersion})):[]}},legacy:{source_version:source.version??1,preserved:Object.fromEntries(Object.entries(source).filter(([k])=>!['anon_id','created_at','updated_at','examV2'].includes(k)))}}}
