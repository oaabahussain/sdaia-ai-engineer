diff --git a/MIGRATIONS.md b/MIGRATIONS.md
new file mode 100644
index 0000000000000000000000000000000000000000..13c6894f75b57339f14947d460d3c747be5f7534
--- /dev/null
+++ b/MIGRATIONS.md
@@ -0,0 +1,5 @@
+# Migration compatibility
+
+The current runtime exposes one public `RuntimeBundleV2` contract from both browser and API adapters. The optional FastAPI service remains development/test scaffolding and is not deployed by this repository.
+
+`GET /v1/bank` is public runtime content and does not require an anonymous identifier. `X-Anon-Id` remains a client-generated correlation identifier for progress, feedback, and event endpoints; it is not authentication or authorization.
diff --git a/api/openapi.yaml b/api/openapi.yaml
index d7e847646001c8c6a33410e7b6f86a3c13171b66..e6e97b7b8dc1546236745f44f23dc2148525bbb0 100644
--- a/api/openapi.yaml
+++ b/api/openapi.yaml
@@ -3,51 +3,39 @@ info:
   title: SDAIA AI Engineer Study Space API
   version: 1.0.0
   description: Future private backend contract. No server is deployed by this repository.
-  license:
-    name: MIT
-    identifier: MIT
+  license: { name: MIT, identifier: MIT }
 servers:
   - url: /v1
+security: []
 components:
-  securitySchemes:
-    AnonId:
-      type: apiKey
-      in: header
-      name: X-Anon-Id
   parameters:
     AnonHeader:
       name: X-Anon-Id
       in: header
       required: true
-      schema:
-        type: string
-        format: uuid
+      description: Client-generated anonymous identifier; not authentication.
+      schema: { type: string, format: uuid }
     AnonPath:
       name: anon_id
       in: path
       required: true
-      schema:
-        type: string
-        format: uuid
+      schema: { type: string, format: uuid }
     IfMatch:
       name: If-Match
       in: header
       required: true
-      schema:
-        type: string
+      schema: { type: string }
   responses:
     Error:
       description: Error response
       content:
         application/json:
-          schema:
-            $ref: ../data/schema/error.schema.json
+          schema: { $ref: ../data/schema/error.schema.json }
 paths:
   /health:
     get:
       operationId: health
       summary: Check service liveness
-      security: []
       responses:
         '200':
           description: Liveness
@@ -56,22 +44,18 @@ paths:
               schema:
                 type: object
                 required: [status]
-                properties:
-                  status: { type: string, const: ok }
+                properties: { status: { type: string, const: ok } }
                 additionalProperties: false
   /bank:
     get:
       operationId: getBank
-      summary: Load the full study bank
-      security: [{ AnonId: [] }]
-      parameters: [{ $ref: '#/components/parameters/AnonHeader' }]
+      summary: Load the public runtime bundle
       responses:
         '200':
-          description: Full study bank
+          description: Canonical public RuntimeBundleV2
           content:
             application/json:
-              schema: { $ref: ../data/schema/bank.schema.json }
-        '400': { $ref: '#/components/responses/Error' }
+              schema: { $ref: ../data/schema/runtime-bundle.schema.json }
   /progress/{anon_id}:
     parameters:
       - { $ref: '#/components/parameters/AnonPath' }
@@ -79,45 +63,40 @@ paths:
     get:
       operationId: getProgress
       summary: Load anonymous progress
-      security: [{ AnonId: [] }]
-      description: X-Anon-Id must match anon_id.
+      description: X-Anon-Id is a client-generated identifier, not authentication, and must match anon_id.
       responses:
         '200':
           description: Saved state
-          headers:
-            ETag: { schema: { type: string } }
+          headers: { ETag: { schema: { type: string } } }
           content:
             application/json:
-              schema: { $ref: ../data/schema/state.schema.json }
+              schema: { $ref: ../data/schema/state-v2.schema.json }
         '400': { $ref: '#/components/responses/Error' }
         '404': { $ref: '#/components/responses/Error' }
     put:
       operationId: putProgress
       summary: Replace anonymous progress
-      security: [{ AnonId: [] }]
-      description: Full replace. X-Anon-Id and state anon_id must match anon_id. If-Match carries the current numeric version; use 0 for first create.
+      description: Full replace. X-Anon-Id is not authentication; it and state anon_id must match anon_id. If-Match carries the current numeric version; use 0 for first create.
       parameters:
         - { $ref: '#/components/parameters/IfMatch' }
       requestBody:
         required: true
         content:
           application/json:
-            schema: { $ref: ../data/schema/state.schema.json }
+            schema: { $ref: ../data/schema/state-v2.schema.json }
       responses:
         '200':
           description: Saved state
-          headers:
-            ETag: { schema: { type: string } }
+          headers: { ETag: { schema: { type: string } } }
           content:
             application/json:
-              schema: { $ref: ../data/schema/state.schema.json }
+              schema: { $ref: ../data/schema/state-v2.schema.json }
         '400': { $ref: '#/components/responses/Error' }
         '409': { $ref: '#/components/responses/Error' }
   /feedback:
     post:
       operationId: postFeedback
       summary: Submit anonymous question feedback
-      security: [{ AnonId: [] }]
       parameters: [{ $ref: '#/components/parameters/AnonHeader' }]
       requestBody:
         required: true
@@ -132,16 +111,13 @@ paths:
               schema:
                 type: object
                 required: [ok, ref]
-                properties:
-                  ok: { type: boolean, const: true }
-                  ref: { type: string }
+                properties: { ok: { type: boolean, const: true }, ref: { type: string } }
                 additionalProperties: false
         '400': { $ref: '#/components/responses/Error' }
   /events:
     post:
       operationId: postEvents
       summary: Submit anonymous event batch
-      security: [{ AnonId: [] }]
       parameters: [{ $ref: '#/components/parameters/AnonHeader' }]
       requestBody:
         required: true
@@ -156,7 +132,6 @@ paths:
               schema:
                 type: object
                 required: [ok]
-                properties:
-                  ok: { type: boolean, const: true }
+                properties: { ok: { type: boolean, const: true } }
                 additionalProperties: false
         '400': { $ref: '#/components/responses/Error' }
diff --git a/data/schema/exam-profile.schema.json b/data/schema/exam-profile.schema.json
new file mode 100644
index 0000000000000000000000000000000000000000..98685b4e0e7d47935ed858bec8de9be35878a231
--- /dev/null
+++ b/data/schema/exam-profile.schema.json
@@ -0,0 +1 @@
+{"$schema":"http://json-schema.org/draft-07/schema#","$id":"exam-profile.schema.json","type":"object","required":["schema_version","id","track_id","version","evidence_status","question_count","section_sizes","weights"],"properties":{"schema_version":{"const":1},"id":{"type":"string","minLength":1},"track_id":{"type":"string","minLength":1},"version":{"type":"string","minLength":1},"evidence_status":{"enum":["official-verified","project-reference-unverified","observational"]},"question_count":{"type":"integer","minimum":1},"section_sizes":{"type":"array","minItems":1,"uniqueItems":true,"items":{"anyOf":[{"type":"integer","minimum":1},{"const":"all"}]}},"weights":{"type":"object","minProperties":1,"additionalProperties":{"type":"number","minimum":0}}},"additionalProperties":false}
diff --git a/data/schema/rendered-question.schema.json b/data/schema/rendered-question.schema.json
new file mode 100644
index 0000000000000000000000000000000000000000..397160fe5e72d0c391e92d7527f51305aae773a4
--- /dev/null
+++ b/data/schema/rendered-question.schema.json
@@ -0,0 +1 @@
